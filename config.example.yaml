server:
  port: "8080"
  mode: "debug" # "debug" or "release"

database:
  host: "localhost"
  port: 5432
  user: "freedomcry"
  password: "freedomcry_secret"
  dbname: "freedomcry_db"
  sslmode: "disable"

redis:
  addr: "localhost:6379"
  password: ""
  db: 0

jwt:
  secret: "" # REQUIRED: Set via JWT_SECRET environment variable
  expiry: 72h

app:
  base_url: "http://localhost:8080"
  node_secret: "" # Set via NODE_SECRET
  default_dns: "1.1.1.1, 8.8.8.8"
